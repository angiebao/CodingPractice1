# Linked List
#
# 套路
    # Cycle detection
    # Reverse
    # Sort
    # Merge list
    # Get middle node
    # Use dummy head
#
# linkedlist vs. array:
    # use linkedlist when dynamically allocate memory, also good for insert (two directional linkedlist). or for single linkedList when inserting in the beginning of the list
    # search, insert, delet… basic operations
    # use array when need to know particular index’s value(random access), or get the length,
    #
# Heap
#
    # K-way merge sort
    # 扫描线: swipe line, mark all the  timestamp and if it is (start or end) in a timeline, swipe the entire time line, keep updating results with the timestamp
    # start 要放在end 前面
    # lambda expression for comparator
    #   Queue<ListNode> pq = new PriorityQueue<>((a, b) -> a.val - b.val);
    #
    # treeSet .floor keyi
    # set.floor ouwenti
    # treeset<Integer> set =
    #
    # 找中位数： datastream, 数据流 必须要用heap,因为 不能一下子得到所有的元素
    # Dijkstra algorithm (求最短路径  BFS+Heap解法)

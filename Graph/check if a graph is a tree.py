# 存图：
# 1.adjacency list
# 2.
# 压缩路径， union find

def isValidTree(input):
    if not len(input) ==0:
        return True
    Nodes = getNodes(input)
    nodeCount =len(nodes)
    if node COunt -1 !=len(input):
        return False
    Parents = [i for i in range(nodeCount)]

    for edge in input:
        Start, end = edge[0],

